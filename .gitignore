node_modules
dist
release
.DS_Store
*.log

# erase-suite output (renders, exported PDFs, reports)
tests/.artifacts/
release/
tests/.art/

# ctx-wire spool + scratch probes
tests/.b/

# esbuild bundles from adversarial-review repro scripts
/.art/
tests/art/
tests/.a*/
*.bundle.mjs
site/app/

# Local benchmark documents. Real third-party files: they stay on this machine,
# never in a commit, and therefore never in the published export.
tests/fixtures/bench/
dist-electron/
